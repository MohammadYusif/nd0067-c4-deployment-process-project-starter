version: 2.1

jobs:
  deploy:
    docker:
      - image: cimg/node:20.20.0 # Updated to the latest Node.js version
    steps:
      - checkout
      - run:
          name: Deploy App
          command: |
            npm install
            npm run api:deploy && npm run frontend:deploy

workflows:
  version: 2
  deploy:
    jobs:
      - deploy